version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      # Lấy ID tài khoản AWS một cách tường minh và gán vào biến CUSTOM_AWS_ACCOUNT_ID
      - export CUSTOM_AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - echo "AWS Account ID: $CUSTOM_AWS_ACCOUNT_ID" # Để kiểm tra trong log

      # Sử dụng CUSTOM_AWS_ACCOUNT_ID để định nghĩa REPOSITORY_URI
      - REPOSITORY_URI=$CUSTOM_AWS_ACCOUNT_ID.dkr.ecr.ap-southeast-1.amazonaws.com/qairline-backend-repo # THAY BẰNG URI ECR CỦA BẠN (nếu tên repo khác)

      # Bây giờ sử dụng REPOSITORY_URI đã được giải quyết cho lệnh docker login
      - aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin $REPOSITORY_URI

  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image for backend...
      - docker build -t $REPOSITORY_URI:latest ./backend/
      - docker tag $REPOSITORY_URI:latest $REPOSITORY_URI:$CODEBUILD_RESOLVED_SOURCE_VERSION

  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker images to ECR...
      - docker push $REPOSITORY_URI:latest
      - docker push $REPOSITORY_URI:$CODEBUILD_RESOLVED_SOURCE_VERSION
      - echo Copying deployment files...
      - cp backend/docker-compose.yml .
      - cp backend/nginx/nginx.conf .
      - cp backend/ci-cd-configs/appspec.yml .
      - cp -r backend/ci-cd-configs/scripts .

artifacts:
  files:
    - docker-compose.yml
    - nginx.conf
    - appspec.yml
    - scripts/**/*
  discard-paths: yes
