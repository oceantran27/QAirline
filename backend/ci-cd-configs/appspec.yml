version: 0.0
os: linux
files:
  - source: /docker-compose.yml
    destination: /home/ec2-user/qairline-backend
  - source: /nginx.conf
    destination: /home/ec2-user/qairline-backend/nginx

permissions:
  - object: /home/ec2-user/qairline-backend
    pattern: "**"
    owner: ec2-user
    group: ec2-user

hooks:
  BeforeInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/stop_containers.sh
      timeout: 180
      runas: ec2-user
  ApplicationStart:
    - location: scripts/start_containers.sh
      timeout: 360
      runas: ec2-user
  ValidateService:
    - location: scripts/validate_service.sh
      timeout: 300
      runas: ec2-user
